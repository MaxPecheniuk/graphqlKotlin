type Post {
    id: ID!
    title: String
    description: String
    date: Int
    commentsIds: [ID!]
    Test:String
}
type Author{
    id: ID!
    avatar: String
    name: String
}

type Query {
    posts(title: String): [Post]
    authors: [Author]
    postById(id: ID!): Post
}
